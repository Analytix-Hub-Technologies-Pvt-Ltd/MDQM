import BaseRoleDashboard from "./BaseRoleDashboard";

export default function AuditorDashboard() {
  return <BaseRoleDashboard endpoint="auditor" title="Auditor Compliance Console" subtitle="Audit evidence, access traces, and policy exceptions across domains." />;
}
